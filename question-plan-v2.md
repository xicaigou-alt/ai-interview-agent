# 面试问题方案设计（v2）

> 状态：设计定稿 + 已实现（2025-06），typecheck 与单元测试通过。
> 涉及模块：`src/core/planner/plan-interview.ts`、`src/core/interview/engine.ts`、`src/core/state/candidate-state.ts`、`src/core/engine-types.ts`、`src/core/generate/generate-question.ts`、`src/app/interview/[id]/brief/page.tsx`。

---

## 1. 背景与问题

### 1.1 现状

当前"问题方案"由两层组成：

1. **Gap 分析**产出 `highPriorityTopics`（技术主题，如 RAG / Agent / Prompt 工程 / SQL / 产品方案设计）。
2. **Planner** 将其转为扁平列表 `priorityTopics`，引擎按 topic 逐题出题（`selectNextTopic` 只消费 `priorityTopics`）。

### 1.2 发现的问题（按时间线）

| # | 问题 | 根因 | 状态 |
|---|---|---|---|
| P1 | 提问出现简历上没有的内容（如"企业知识库Agent"） | 检索命中导入面经中"原答主的项目锚点"，个性化环节在此基础上二次放大 | ✅ 已修（去锚点 + 简历事实白名单） |
| P2 | 同一场出现相同/近似题（Q4==Q5） | 多个 topic 命中同一批知识库题；无硬去重；模拟题回写造成污染循环 | ✅ 已修（检索排除已用题源与 SIMULATED_INTERVIEW + 文本/语义硬去重 + 回写去个性化底题） |
| P3 | **5 道主问题全部打在同一个最相关项目上，且主问题形态变成"深挖题"** | `InterviewPlan.sections` 的维度配比（RESUME_DEEP_DIVE / AI_KNOWLEDGE / PRODUCT_DESIGN / JD_GAP / BEHAVIORAL）**只展示不执行**；gap 的 topic 天然向简历里最贴合 JD 的项目聚集，每个技术切面都变成一道主问题；主问题与追问职责混淆 | 🔲 **本文档目标** |

### 1.3 真实面试的问题结构（对齐基准）

- **主问题 = 宽入口的"开场题"**：基础知识（不绑简历）→ 逐段实习 → 项目介绍 → 行为动机。
- **深挖 = 追问环节**：面试官基于候选人回答内容动态往下钻（技术细节、数据真实性、个人贡献等）。
- 追问跟随回答走，不预先铺开。

---

## 2. 设计目标

1. 主问题覆盖**不同维度**（基础 / 实习逐段 / 项目 / 行为），杜绝"全打在一个项目上"。
2. 主问题一律**宽入口**；技术深挖只出现在**追问**。
3. **与 JD 相关度优先**：实习/项目覆盖范围按相关度取舍。
4. 任意题目数（3~30）自适应：骨架不足时循环加题，骨架超出时裁剪。

---

## 3. 主问题骨架（已确认）

按以下顺序出题：

| 顺序 | 维度 | 主问题形态（宽入口） | 数量 |
|---|---|---|---|
| 1 | **KNOWLEDGE 基础知识** | 通用知识题，**不绑简历**（对应 JD 硬性要求：AIGC / Prompt 工程 / RAG / Agent / SQL·Python 等） | 1 |
| 2 | **EXPERIENCE 逐段实习** | "介绍一下你在【公司】的【岗位】实习主要做了什么？" | 每段 1 题，按时间倒序 |
| 3 | **PROJECT 项目** | "介绍一下你的【项目】：为什么做、解决什么问题、你在其中的角色？" | 只选与 JD 最相关的 1~2 个 |
| 4 | **BEHAVIORAL 行为动机** | "为什么想做 AI 产品经理？讲讲你的职业规划。" | 1 |

### 3.1 主问题模板（按维度）

- **KNOWLEDGE**：由 JD `priorityRequirements` + 知识点生成通用知识题；**禁止**绑定简历经历。
- **EXPERIENCE**：固定模板 + 真实公司/岗位（来自简历 `experience`，不含时间）。
- **PROJECT**：固定模板 + 真实项目名（来自简历 `projects`，取与 JD 最相关者）。
- **BEHAVIORAL**：动机 / 规划 / 团队协作等行为类，宽入口。

---

## 4. 数量分配规则

### 4.1 默认配比（target = 5）

**基础 1 + 实习 2 + 项目 1 + 行为 1**（简历 2 段实习 + 1~2 个项目时）。

### 4.2 骨架超出 target 时（裁剪）

1. **必留**：基础 1 + 行为 1。
2. 实习/项目按"与 JD 相关度"从低到高裁剪（相关度依据 Gap 分析的 strongMatch / weakMatch）。
3. 裁剪顺序：项目 → 实习（相关度最低的先裁）。

### 4.3 骨架不足 target 时（补足）

进入 §6 超量扩展循环。

---

## 5. 深挖与追问（已确认）

- **主问题不预置深挖**。
- 技术细节（RAG 检索流程、Agent 决策逻辑、Skill 编写、Prompt 迭代、API 编排问题等）**全部由追问引擎基于候选人回答动态生成**（现有 Decision Engine：DEEP_DIVE / TECHNICAL / DATA_VALIDATION / OWNERSHIP_CHECK / COUNTERFACTUAL 等）。
- 追问上限维持现状（每个主问题最多 2 次追问，达上限强制推进）。

---

## 6. 超量扩展循环（已确认）

第一轮覆盖全部骨架维度后，按优先级循环加题：

1. **知识题**：换一个 JD 知识点。
2. **已覆盖经历/项目换角度深挖**：如"如果让你重新设计 X，你会怎么做？"——以主问题形态问，但角度与前一轮不同。
3. **行为扩展**：团队协作、抗压、职业规划延伸。
4. **结合 JD 业务场景的情景题**：如"假设我们要做【JD 中的业务场景】，你会如何设计方案？"

循环直至达到 target。

---

## 7. 数据模型与实现落点

### 7.1 InterviewPlan 结构升级

```
InterviewPlan {
  durationMinutes, difficulty, primaryQuestionTarget,
  sections[]            // 保留（展示用）
  mainQuestions[] {     // 新增：结构化主问题清单（出题顺序即数组顺序）
    dimension: "KNOWLEDGE" | "EXPERIENCE" | "PROJECT" | "BEHAVIORAL" | "JD_SCENARIO",
    target?: string      // 指向：公司/岗位（EXPERIENCE）、项目名（PROJECT）、知识点（KNOWLEDGE）、空（BEHAVIORAL）
  },
  knowledgeTopics[]      // 由旧 priorityTopics 降级而来：追问与知识题的知识点池
}
```

- `selectNextTopic` 改为按 `mainQuestions` 数组顺序推进（已覆盖的跳过），不再消费 `priorityTopics`。
- 超量加题在 planner 阶段就展开成完整 `mainQuestions` 列表（target 已知）。

### 7.2 生成与个性化衔接

- **KNOWLEDGE**：`generateQuestion` 生成通用知识题 → 个性化环节**不绑定简历**（白名单为空则只问知识点）。
- **EXPERIENCE / PROJECT**：模板化主问题直接生成 → 个性化环节校验"目标公司/项目必须在简历白名单内"，防止锚点漂移。
- **BEHAVIORAL / JD_SCENARIO**：模板化 + JD 背景。
- 既有机制继续生效：去锚点 + 简历事实白名单、文本/语义硬去重、检索排除已用题源与 SIMULATED_INTERVIEW、回写去个性化底题。

### 7.3 简报页

"面试计划"区块改为展示 `mainQuestions` 清单（维度 + 指向），替代/补充现在的 sections 权重展示。

---

## 8. 验收标准

1. target=5 时，5 道主问题覆盖 ≥ 4 个不同维度/经历段（基础 / 每段实习 / 项目 / 行为）。
2. 同一项目不出现 >1 道主问题（target ≤ 骨架数时）；target 更大时换角度加题不与原角度重复。
3. 所有主问题均为宽入口（不含"请具体讲讲 XX 的实现细节"这类深挖形态）。
4. 深挖只出现在追问环节，且追问内容与候选人回答强相关。
5. 不出现简历外锚点（如"企业知识库Agent"）；不出现相同/近似主问题。
6. 知识题不绑定简历经历；EXPERIENCE 主问题为职责导向模板（"主要负责的工作"），成果/亮点通过追问挖掘。

---

## 9. 关键规则定稿（2025-06 评审确认）

### 9.1 KNOWLEDGE 知识点映射（双源知识池 + 排序）

1. **知识点池** = JD `priorityRequirements`（明示硬性要求）为主 + AI/Technical 类 competencies 中与前者不重复者补充；去重后一般 5~8 个。
2. 每道知识题聚焦 **1 个知识点**（一次一问）。
3. **排序规则**：JD 权重高者优先 → 与简历 `missingCapabilities` / 历史弱项交集者提前（优先考简历缺失/弱项）。
4. 题目可带 **JD 业务语境**（一句话场景，如"结合企业知识库场景"），但**绝不带简历锚点**。
5. 知识题总数 ≤ 知识点池大小；超量循环按序取下一个知识点。

### 9.2 EXPERIENCE 主问题模板（职责导向）

- 模板：**"介绍一下你在【公司】的【岗位】实习主要负责的工作。"**
- 单点职责导向，符合"宽入口 + 深挖走追问"；成果量化、个人贡献由追问引擎基于回答动态挖掘。
- 每段实习一题，按时间倒序。

### 9.3 历史弱项影响（提权不改结构）

- 骨架维度结构不变（基础/实习/项目/行为都要覆盖），弱项**不砍维度**。
- 提权三处：
  1. KNOWLEDGE 知识点排序提前（弱项对应知识点当第 1 题）；
  2. 超量循环优先加"弱项相关"的换角度深挖题；
  3. 追问侧重弱项（现有 Decision Engine 基于 candidateState 消费弱项，机制不变）。
- 配比微调仅限：弱项明确集中在基础知识时，KNOWLEDGE 1→2（从 BEHAVIORAL 减 1），其余情况配比不动。

### 9.4 简报页信息架构（最终版）

三段式 + 主问题清单：

1. **候选人画像**：个人技能 / 实习经历（公司·岗位·职责）/ 项目经历 / 亮点。
2. **匹配与差距**：强匹配 / 弱匹配 / 缺失能力 / 简历风险 / 优先考察。
3. **面试计划**：
   - 时长 · 目标题数 · 难度；
   - **主问题清单**（按出题顺序：`Q1 [知识] RAG 原理与适用场景`、`Q2 [实习] 腾讯·AI产品实习`…；只显示"维度 + 指向"，**不显示完整题目**，保留模拟感）；
   - 追问说明一句："深挖将在回答后基于你的回答动态进行"；
   - sections 权重折叠或移除（仅作调试信息）。
4. 开始面试按钮。
